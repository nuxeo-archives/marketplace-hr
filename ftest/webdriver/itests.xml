<project name="nuxeo-sample-tests"
         xmlns:nx="urn:nuxeo-build"
         xmlns:artifact="urn:nuxeo-artifact">
  <taskdef resource="org/nuxeo/build/antlib.xml" uri="urn:nuxeo-build" />
  <taskdef resource="org/nuxeo/build/artifact/antlib.xml"
           uri="urn:nuxeo-artifact" />
  <taskdef resource="net/sf/antcontrib/antlib.xml" />

  <property name="out.dir" value="${maven.project.build.directory}" />
  <unzip dest="${out.dir}/" overwrite="false">
    <artifact:resolveFile key="org.nuxeo:nuxeo-ftest::zip" />
  </unzip>
  <import file="${out.dir}/nuxeo-ftest.xml" />

  <!-- named market-place instead of marketplace to work around NXCONNECT-279 -->
  <property name="mp.install"
            value="file:${out.dir}/market-place-hr-mp-${nuxeo.hr.version}.zip" />
  <!-- Override default prepare-environment target to make it download your MP -->
  <target name="prepare-environment"
          depends="_init,prepare-db,prepare-tomcat,prepare-jboss">
    <copy todir="${out.dir}">
      <artifact:resolveFile key="com.nuxeo.human.resources:market-place-hr-mp::zip" />
    </copy>
  </target>

</project>